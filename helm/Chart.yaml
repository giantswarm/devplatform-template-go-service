apiVersion: v2
name: devplatform-template-go-service
description: A Helm chart for Kubernetes
type: application
home: https://github.com/giantswarm/devplatform-template-go-service/
maintainers:
  - name: honey badger team at Giant Swarm
    email: honeybadger@giantswarm.io
icon: https://s.giantswarm.io/app-icons/giantswarm/1/dark.svg

# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates, including the app version.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
# When built with https://github.com/giantswarm/app-build-suite,
# the version might be automatically set based on git tags discovery.
version: 0.1.0

# This is the version number of the application being deployed. This version number should be
# incremented each time you make changes to the application. Versions are not expected to
# follow Semantic Versioning. They should reflect the version the application is using.
# It is recommended to use it with quotes.
# When built with https://github.com/giantswarm/app-build-suite,
# the appVersion might be automatically set based on git tags discovery.
appVersion: "1.16.0"
